apiVersion: v1
kind: Service
metadata:
  name: express-app-svc
  namespace: cicd-platform
spec:
  selector:
    app: express-app
  ports:
    - port: 80
      targetPort: 3000
      name: http
  type: ClusterIP